<!DOCTYPE HTML>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="shortcut icon" type="image/png" href="{{ asset('favicon.ico') }}" />
        <title>{% block title 'Znieh Games' %}</title>

        {% block stylesheets %}
            {#<link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}"/>#}
            <link rel="stylesheet" href="{{ asset('css/jquery-ui-1.10.4.custom.css') }}"/>
            <!-- Generated by sass : -->
            <link rel="stylesheet" href="{{ asset('assets/css/layout.css') }}"/>

            {#<link rel="stylesheet" href="{{ asset('css/style.css') }}"/>#}
        {% endblock %}

        <!-- HTML5 Shim and Respond.js add IE8 support of HTML5 elements and media queries -->
        {% include 'BraincraftedBootstrapBundle::ie8-support.html.twig' %}

    </head>
    <body {% block initCreateJS '' %}>
        <div id="wrap">

            {% block navbar %}
            <!-- Fixed navbar -->
                <div class="navbar navbar-default navbar-fixed-top" role="navigation">
                  <div class="container">
                    <div class="navbar-header">
                      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                      </button>
                      <a class="navbar-brand" href="{{ path('znieh_public_public_index') }}">Znieh Games</a>
                    </div>
                    <div class="navbar-collapse collapse">
                      <ul class="nav navbar-nav">
                        <li class="active"><a href="#">Home</a></li>
                        <li><a href="{{ path('live_blog_public_index') }}">Blog</a></li>
                        <li><a href="{{ path('znieh_villagegame_public_index') }}">Village</a></li>
                        {# <li><a href="{{ path('znieh_coregame_default_index') }}">Jouer</a></li> #}
                        {% if is_granted("ROLE_USER") %}
                        <li class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Ressources <b class="caret"></b></a>
                          <ul class="dropdown-menu">
                              <li><a href="#"><span id="res-sto">{{ app.user.ressource.sto }}</span> <img src="{{ asset('img/ressources/sto.png') }}" alt=""> sto</a></li>
                              <li><a href="#">{{ app.user.ressource.gold }} <img src="{{ asset('img/ressources/po.png') }}" alt=""> po</a></li>
                              <li><a href="#">{{ app.user.ressource.copper }} <img src="{{ asset('img/ressources/cuivre.png') }}" alt=""> cuivre</a></li>
                              <li><a href="#">{{ app.user.ressource.bronze }} <img src="{{ asset('img/ressources/bronze.png') }}" alt=""> bronze</a></li>
                              <li><a href="#">{{ app.user.ressource.minorCloth }} <img src="{{ asset('img/ressources/etoffeMineure.png') }}" alt=""> etoffe mineure</a></li>
                              <li><a href="#">{{ app.user.ressource.standardCloth }} <img src="{{ asset('img/ressources/etoffeStandart.png') }}" alt=""> etoffe standard</a></li>
                              <li><a href="#">{{ app.user.ressource.roughLeather }} <img src="{{ asset('img/ressources/cuirReche.png') }}" alt=""> cuir rêche</a></li>
                              <li><a href="#">{{ app.user.ressource.rawLeather }} <img src="{{ asset('img/ressources/cuirBrut.png') }}" alt=""> cuir brut</a></li>
                              <li><a href="#">{{ app.user.ressource.cedarWood }} <img src="{{ asset('img/ressources/boisChene.png') }}" alt=""> bois chêne</a></li>
                              <li><a href="#">{{ app.user.ressource.oakWood }} <img src="{{ asset('img/ressources/boisReche.png') }}" alt=""> bois rêche</a></li>
                          </ul>
                      </li>
                      {% endif %}
                      </ul>
                      <ul class="nav navbar-nav navbar-right">
                        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                          <li><a href="#">{{ app.user.username }}</a></li>
                        {% else %}
                        <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                        {% endif %}
                      </ul>
                    </div><!--/.nav-collapse -->
                  </div>
                </div>
            {% endblock %}

            {% block body '' %} <!-- end block body -->

            <br><br><br>

            {% block requirejs %}
                {{ include('::_requirejs.html.twig', { 'module': false }) }}
            {% endblock %} <!-- end requirejs block -->

            {% block javascripts %}
            <!-- TODO remove and put into requirejs -->
                <script type="text/javascript" src="http://momentjs.com/downloads/moment-with-langs.min.js"></script>
            {% endblock %}

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                {% block chat %}
                    {% include '::chat.html.twig' %}
                {% endblock %}
            {% endif %}

            {% block footer %}
                {% include '::footer.html.twig' %}
            {% endblock %} <!-- end block footer -->

        </div>

            {% block createJs '' %}

            <!-- Facebook Connect -->
            <script>
                function goLogIn() {
                    window.location.href = "{{ path('_security_check') }}";
                }

                function onFbInit() {
                    if (typeof(FB) != 'undefined' && FB != null ) {

                        $('#facebook-login').on('click', function(e) {
                            FB.login(function(response) {
                                if (response.authResponse) {
                                    setTimeout(goLogIn, 500);
                                }
                             }, {scope: 'email'});
                            e.preventDefault();
                        });

                        $('#facebook-logout').on('click', function(e) {
                            FB.logout(null);
                        });

                        FB.Event.subscribe('auth.statusChange', function(response) {
                            if (response.session || response.authResponse) {
                                setTimeout(goLogIn, 500);
                            } else {
                                window.location.href = "{{ path('_security_logout') }}";
                            }
                        });
                    }
                }
            </script>

            <!-- Google Analytics -->
            <script>
              (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
              (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
              m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
              })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

              ga('create', '{{ ga_tracking }}', 'efrei.fr');
              ga('send', 'pageview');
            </script>
    </body>
</html>
