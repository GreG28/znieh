{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Unit list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cost</th>
                <th>Name</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('village_create_unit_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cost }}</td>
                <td>{{ entity.name }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('village_create_unit_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('village_create_unit_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('village_create_unit_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
