{% extends '::base.html.twig' %}

{% block body -%}
<div class="container">
      <h1>Création d'une nouvelle unité</h1>

      {{ form(form, { 'style': 'horizontal', 'label_col': 4, 'widget_col': 8 }) }}

          <ul class="record_actions">
      <li>
          <a href="{{ path('village_create_unit') }}">
              Back to the list
          </a>
      </li>
  </ul>
</div>
{% endblock %}
