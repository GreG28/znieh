{% extends '::base.html.twig' %}

{% block body -%}

<div class="container">
    <h1>Team</h1>
   <table class="record_properties">
         <tbody>
             <tr>
                 <th>Id</th>
                 <td>{{ entity.id }}</td>
             </tr>
             <tr>
                 <th>Name</th>
                 <td>{{ entity.name }}</td>
             </tr>
         </tbody>
     </table>
         <a href="{{ path('znieh_unitgame_team_index') }}">
             Retour
         </a>
         <a href="{{ path('village_create_team_edit', { 'id': entity.id }) }}">
             Editer
         </a>
     {{ form(delete_form) }}
</div>
{% endblock %}
