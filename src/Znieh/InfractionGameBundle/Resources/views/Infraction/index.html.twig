{% extends '::admin.html.twig' %}

{% block body -%}
<div class="container">
    <h1>Liste des infractions</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Joueur</th>
                <th>Libellé</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('fos_user_profile_show', { "id": entity.user.id }) }}"> {{ entity.user.username }}</a></td>
                <td><a href="{{ path('admin_infractions_show', { 'id': entity.id }) }}">{{ entity.type.title }}</a></td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_infractions_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_infractions_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_infractions_new') }}">
                Ajouter une nouvelle infraction
            </a>
        </li>
    </ul>
</div>

{% endblock %}
