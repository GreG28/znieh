{% extends '::base.html.twig' %}

{% block body %}

  <div class="container">
    <div class="row">
      <div class="span8">
        <h2 class="section-title">{{ post.title }}</h2>

          {{ post.content | raw }}

      </div>
    </div>
  </div>

{% endblock %}
